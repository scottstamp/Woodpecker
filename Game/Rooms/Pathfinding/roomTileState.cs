namespace Woodpecker.Game.Rooms.Pathfinding
{
    /// <summary>
    /// An enum containing values for the state of a tile in a room instance's map.
    /// </summary>
    public enum roomTileState
    {
        Free = 0,
        Blocked = 1,
        Interactive = 2,
    }
}
